# Daglo MCP - Environment Variables

# Required for all tests and scripts
DAGLO_EMAIL=your-email@example.com
DAGLO_PASSWORD=your-password

# Optional: Custom API base URL (defaults to https://backend.daglo.ai)
# DAGLO_API_BASE=https://backend.daglo.ai

# Optional: Refresh token (if you want to reuse existing tokens)
# DAGLO_REFRESH_TOKEN=your-refresh-token-here
