import { Command } from "commander";
import { DagloApiClient } from "../api/client.js";

export const registerBoardCommand = (_program: Command, _client: DagloApiClient) => {
  // stub — real implementation in Task 3.7
};
